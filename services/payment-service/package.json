{
  "name": "@quantummin/payment-service",
  "version": "0.1.0",
  "type": "commonjs",
  "main": "dist/webhook-handler.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev:webhook": "ts-node-dev --respawn --transpile-only src/webhook-handler.ts",
    "dev": "ts-node-dev --respawn --transpile-only src/webhook-handler.ts"
  },
  "dependencies": {
    "@quantummin/shared": "*",
    "express": "^4.21.2",
    "stripe": "^16.12.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.25",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.3"
  }
}